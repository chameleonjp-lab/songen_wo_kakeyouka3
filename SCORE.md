# スコア・戦闘ルール

## メーター

| メーター | 初期／上限 | 役割 |
|---|---:|---|
| Vigor（体力） | 100 / 100 | 0になると敗北 |
| Rage（怒気） | 0 / 100 | 攻撃・被弾・防御等で増え、100で怒破を発動できる |
| Dignity（尊厳） | 100 / 100 | 頭・被弾などで減る。0になるとPoop head変身と復帰補正 |
| Combo | 0 / 上限なし（得点倍率は20段階まで） | 2.2秒ヒットがないとリセット |

## ヒット位置

`HitLocations` の基準値は次の通りです。ランタイムは監査済みGLBの名前から `Head`／`Torso`／`HeartBody` 等のメッシュ中心を取得し、頭0.54m、胴体0.92m、心臓0.34mの空間volumeで軌道との交差を確認します。心臓は露出状態 (`heartExposed`) が確認できない場合、胴体へ安全にフォールバックします。

| 位置 | 体力ダメージ倍率 | 得点倍率 | 尊厳ダメージ | stagger倍率 | critical |
|---|---:|---:|---:|---:|---|
| 胴体 | 1.00 | 1.00 | 1 | 1.00 | いいえ |
| 頭 | 0.75 | 1.55 | 6 | 1.35 | はい |
| 心臓（開放時） | 2.20 | 2.40 | 3 | 1.55 | はい |

## 得点台帳

`Score.ts` のデフォルト値は次の通りです。

- ヒット: 胴体25点、頭100点、確認済み心臓300点。最初に使う技は+50点。
- コンボ: 現在コンボ1段ごとに8%（得点倍率の加算は20段まで）。同じ技を繰り返すと15%刻みで減衰し、倍率は25%未満にならない。
- ジャストガード+200点、攻撃相殺+150点、撃破+1000点（敵のround倍率と現在コンボを加味）。通常ガードは0点。
- 被ダメージ1あたり-10点、尊厳喪失1あたり-10点、Poop head変身1回-250点。
- 各ラウンド撃破時、そのラウンドの実戦時間60秒以内を上限に最大+1500点の短時間ボーナス。幕間と開始前ロード時間はラウンド実戦時間へ含めません。
- ミスはコンボをリセットし、命中数と合わせて結果の命中精度を算出する。被弾・尊厳喪失・変身もコンボをリセットする。

`GameSession` は結果としてスコア、個人ベスト、撃破数、到達round、残体力／尊厳、心臓／頭ヒット、just guard、clash、固有技数、最大コンボ、命中精度、被ダメージ、尊厳喪失、変身回数、グレードを保持します。個人ベストは `songen3.personalBest` として端末内に保存されます。

上表はライブの `GameSession` でも固定基準値として使います。部位ごとの `ResolvedHit.scoreMultiplier` は将来の難易度モード用メタデータとして保持し、心臓 +300／頭部 +100 という現在の明示値へは重ね掛けしません。

## 攻撃バランスの基準値

`CombatBalance` の基準攻撃は、弱1.25（範囲3.0）、強3.8（範囲4.1）、カウンター11.5（範囲6.7）、怒破8.7（パルス式、範囲5.8から拡大）です。アニメーション固有の40技カタログは [GLB_ANIMATION_CATALOG.md](GLB_ANIMATION_CATALOG.md) に分けて管理します。

この表はルールモジュールの基準値です。敵ごとの倍率、実アニメーション長、ガード／回避、心臓開放状態による実効値はランタイムで組み合わされ、変更時は `server/gameplay-rules.test.ts` とブラウザ監査を更新します。

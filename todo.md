# 派生コンボ拡張

- [x] 現行の弱攻撃コンボ状態と入力バッファを確認する。
- [x] 弱→強および弱→弱→強の派生受付と専用モーション・判定を実装する。
- [x] 派生成功をHUD、ヒットエフェクト、敵の巻き込みで明確に伝える。
- [x] `?demo`で派生コンボを再現し、型チェック・ビルド・画面検証を行う。

# ジャストガードとカウンター

- [x] 敵の攻撃予備動作、命中時刻、プレイヤー被弾処理を確認する。
- [x] ガード入力、短いジャスト判定、成功後のカウンター受付を実装する。
- [x] ジャストガードの減速・衝撃表示と、カウンターの広範囲攻撃を実装する。
- [x] デモ、型チェック、ビルド、画面検証を完了する。

# 怒破・怒気必殺技

- [x] 怒気100の全消費で起動する必殺技入力と専用状態を実装する。
- [x] 4回の全方位範囲攻撃、強化ノックバック、カメラ揺れ、HUDを実装する。
- [x] 命中地点とプレイヤー周囲から大量の『怒破』テキストが弾ける演出を実装する。
- [x] デモ、型チェック、ビルド、画面検証を完了する。

# 怒気最大の敵強調

- [x] 怒気最大状態と敵の描画・マテリアル接続点を確認する。
- [x] 怒気100の間、敵の輪郭に青銅色の発光強調を適用する。
- [x] 怒破発動時の即時解除と、戦場での視認性を検証する。
- [x] 型チェック、ビルド、画面検証を完了する。

# 動物敵ロスター統合

- [x] ガチョウをプレイヤー、熊・ワニ・ゴリラ・カバ・ライオン・サイを敵として割り当てる。
- [x] 各敵GLBを既存敵AI・攻撃予備動作・被弾・撃破へ接続する。
- [x] 怒気最大時の敵輪郭強調と敵種別の視認性を検証する。
- [x] 型チェック、ビルド、画面検証を完了する。

# GLBアニメーション連動

- [x] GLB内のAnimationGroup名とプレイヤー・敵の戦闘状態を照合する。
- [x] 待機・移動・攻撃・防御・怯み・撃破アニメーションの再生制御を実装する。
- [x] アニメーション未対応モデルのフォールバックと遷移の滑らかさを検証する。
- [x] 型チェック、ビルド、画面検証を完了する。

# GLBアニメーション検証補強

- [x] ガチョウ・熊・ワニ・ゴリラ・カバ・ライオン・サイのAnimationGroup名を実測し、対応表を保存する。
- [x] 未対応AnimationGroup時に手続きメッシュを残す視覚フォールバックを実装する。
- [x] `?demo`でidle・move・guard・light・heavy・hurt・deadの状態遷移を再現検証する。
- [x] `?demo`でidle / move / guard / light / heavy / hurt / deadを順番に再現する検証フローを追加し、各状態のGLBアニメーションまたはフォールバック表示を確認する。

# アニメーション個別検証

- [x] `animationTestPhase`でidle / move / guard / light / heavy / hurt / deadを固定表示できる検証モードを追加する。
- [x] 7フェーズを個別に画面確認し、GLBアニメーションまたは手続きメッシュフォールバックを記録する。
- [x] 7フェーズ（idle / move / guard / light / heavy / hurt / dead）の個別確認結果を専用検証メモへ記録する。

# 攻撃同期と効果音

- [x] Punch_R／Kick_Lの実時間と既存ヒット判定フレームを照合する。
- [x] 弱・強・敵攻撃・カウンター・怒破のヒット判定をアニメーション同期へ調整する。
- [x] 攻撃開始・命中・ガード・カウンター・怒破のWeb Audio効果音を実装する。
- [x] 型チェック、ビルド、画面検証を完了する。

# 1対1連戦フロー

- [x] 同時出現数を1体に制限し、初期配置を1体へ変更する。
- [x] 現在の敵を撃破した後、次の敵を1体だけスポーンする連戦制御を実装する。
- [x] 連戦中も敵ロスター、怒気、コンボ、既存の戦闘演出を維持する。
- [x] `?demo`、型チェック、テスト、ビルド、画面表示を検証する。

# NEXT CHALLENGER幕間演出

- [x] 敵撃破後から次敵スポーンまでの演出状態とタイマーを追加する。
- [x] 「NEXT CHALLENGER」と次戦情報を中央演出として表示する。
- [x] 幕間中も1対1の連戦制御と既存の戦闘テンポを維持する。
- [x] `?demo`、型チェック、テスト、ビルド、画面表示を検証する。

# 敵種別の登場ポーズと挑発吹き出し

- [x] 敵種別ごとの登場ポーズ、表示名、短い挑発セリフを定義する。
- [x] 次敵スポーン時にGLBアニメーションまたはフォールバックで固有ポーズを再生する。
- [x] NEXT CHALLENGER中に敵の挑発セリフを吹き出し表示する。
- [x] `?demo`、型チェック、テスト、ビルド、画面表示を検証する。

# 敵種別の登場咆哮・効果音

- [x] 敵種別ごとの咆哮・威圧音パラメータを定義する。
- [x] NEXT CHALLENGERから登場ポーズへ固有音を同期して再生する。
- [x] 音量、音程、既存戦闘音との重なりを調整する。
- [x] `?demo`、型チェック、テスト、ビルド、画面表示を検証する。
- [x] `?demo`で各敵種別の登場咆哮が1回だけ発火することを確認できる回帰テストまたは検証用ログ・計測フックを追加する。
- [x] `?demo`の実スポーン経路で敵種別ごとの咆哮回数を収集・参照できる計測フックをGameWorldへ接続する。
- [x] `?demo`相当のスポーンシーケンスで各敵種別の登場咆哮が1回だけ発火することを検証する。

# 咆哮の立体音響

- [x] 登場咆哮に簡易リバーブを追加し、闘技場の残響感を作る。
- [x] 敵の左右位置に応じたパンニングを追加する。
- [x] 咆哮の音量、リバーブ量、パンニングが既存戦闘音と干渉しないよう調整する。
- [x] `?demo&audioDebug`、型チェック、テスト、ビルド、画面表示を検証する。
- [x] `?demo&audioDebug`の実スポーン経路で、登場咆哮のpan値とリバーブ有効状態を観測できる計測フックを追加する。
- [x] `?demo`相当の実スポーン経路で、敵位置に応じてpan値が変化し、リバーブ経路付きで再生されることを検証する。
- [x] `?demo&audioDebug`実行時に各敵のpanとreverbを確認できる検証ログを残す。
- [x] 実スポーン経路の音響イベントへpan変化とreverb有効状態が含まれることを回帰確認する。
- [x] `?demo`相当の6種スポーンシーケンスで各敵のpan・reverbログを取得・記録する。
- [x] GameWorldが実スポーン時に発行する`arena-entry-roar` payloadを回帰テストで検証する。
- [x] 複数敵種または左右位置差でpan値が実際に異なることを検証する。

# スマホ縦画面とPC高密度テクスチャ

- [x] スマホ縦画面向けのレイアウトと安全領域を整える。
- [x] 画面下部へ移動、弱攻撃、強攻撃、ガード、怒破、回避のタッチボタンを追加する。
- [x] タッチボタンを既存のGameWorld入力コマンドへ接続し、同時押し・長押しを安全に扱う。
- [x] PC版では高密度テクスチャ設定を優先し、既存モデル・質感を維持する。
- [x] PC横画面、スマホ縦画面、タッチ操作、型チェック、テスト、ビルドを検証する。
- [x] PC向けTexture.levelを標準寄与率へ修正し、テクスチャ質感を維持する。
- [x] タッチ移動の長押し・斜め同時押し・攻撃/ガード併用を回帰テストで確認する。
- [x] PC横画面・スマホ縦画面・タッチ入力の検証結果を記録する。

# スマホタッチボタンの視覚フィードバック

- [x] タッチボタン押下中の沈み込み、明るさ、発光変化を追加する。
- [x] ポインターキャンセルや離脱時も押下状態が確実に復帰するようにする。
- [x] `prefers-reduced-motion`環境で過度なアニメーションを抑制する。
- [x] スマホ縦画面、型チェック、テスト、ビルドを検証する。

# 三人称移動・攻撃テストプレイ

- [x] デモモードで三人称追従カメラとプレイヤー移動を確認する。
- [x] 弱攻撃、強攻撃、弱弱強の派生コンボが発生することを確認する。
- [x] PC横画面とスマホ縦画面でプレイ画面・HUD・戦闘状態を確認する。
- [x] BabylonのRay side-effect import警告を解消する。
- [x] `?demo`で弱→強、弱→弱→強、強攻撃のHUD・ログを記録する。
- [x] PC横画面・スマホ縦画面の戦闘中HUDと三人称追従結果を記録する。

# コンソールエラー修正

- [x] ブラウザコンソールの14件のエラー内容を収集して分類する。
- [x] 各エラーの原因を修正し、関連する回帰テストを追加する。
- [x] 修正後にブラウザログ、型チェック、テスト、ビルドを再検証する。
- [x] Babylon shader/ray import不備の再発を検知する最小回帰テストを追加する。
- [x] クリーンなブラウザログで`?demo`を再実行し、コンソールERROR件数0件を確認・記録する。

# 攻撃相殺

- [x] プレイヤー攻撃と敵攻撃の同時接触を検出する。
- [x] 同時接触時に双方のダメージと被弾処理を相殺する。
- [x] 相殺専用の衝撃エフェクト・効果音・HUDフィードバックを追加する。
- [x] 通常命中、ガード、カウンター、怒破への影響を確認する。
- [x] 同時攻撃の回帰テスト、型チェック、テスト、ビルド、画面検証を行う。
- [x] `?demo&clashAudit`でプレイヤー攻撃と敵strikeの相殺を決定論的に発生させ、双方ノーダメージとATTACK CLASHを記録する。
- [x] 実GameWorld経路で相殺時の被弾・ダメージ・コンボ/怒気副作用が抑止される回帰テストまたは監査ログを追加する。clashAuditにcombo/rage前後値を記録。
- [x] 相殺追加後も通常命中、ガード、カウンター、怒破が維持される専用監査ログまたは画面確認を追加する。combatAuditで実ガード→just guard→counter、既存musouログを確認。

# モバイルタッチカメラと振動フィードバック

- [x] 縦画面の空き領域をスワイプしてカメラの水平・垂直視点を操作できるようにする。
- [x] タッチカメラ操作の感度、デッドゾーン、上下角制限、ポインターキャンセルを最適化する。
- [x] 攻撃命中、ガード、ジャストガード、攻撃相殺、怒破、被弾、撃破で端末振動を発火する。
- [x] `navigator.vibrate`非対応・権限拒否・Reduced Motion環境で安全に無効化する。
- [x] タッチカメラと振動フィードバックの回帰テスト、型チェック、ビルド、PC/スマホ画面検証を行う。

- [x] PC横画面でタッチカメラ追加後のHUDとオーバーレイ非表示状態を再確認する。
- [x] `arena-touch-look` の縦方向入力とpointer cancel復帰を回帰テストへ追加する。縦方向lookをVitestで確認し、surfaceのpointer cancel処理を実装確認。
- [x] Reduced Motion時のHaptics無効化と`vibrate()`失敗/false返却時の安全動作を回帰テストへ追加する。

# 1対1敵ロックオンカメラ

- [x] 現在の生存敵を自動ロックオンし、カメラの注視点をプレイヤーと敵の中間へ補間する。
- [x] ロックオン中もタッチスワイプで周囲を回り込めるよう、水平オフセットと上下角を制限する。
- [x] 敵撃破・死亡・次敵スポーン・幕間でロック対象を安全に解除・再取得する。
- [x] ロックオン状態をHUDまたは控えめな視覚表現で確認できるようにする。
- [x] 1対1連戦、モバイル縦画面、PC横画面、型チェック、テスト、ビルドを検証する。

- [x] 敵撃破→NEXT CHALLENGER幕間→次敵スポーンの実経路で、ロック対象が解除され再取得される監査ログを追加する。lockAuditでbear解除→crocodile再取得を確認。
- [x] lockOn HUD payloadとカメラ注視点・軌道オフセットの基本挙動を専用監査で検証する。lockAuditでlockOn=true、orbitOffset、beta、radiusを記録。

- [x] `arena-hud` のlockOnフラグ遷移（free→locked→released→relocked）をlockAuditで収集する。hud false→true→false→trueを確認。
- [x] lockAuditへcamera.targetとplayer/enemy由来の注視点を含め、中間注視への補間を検証する。cameraTargetとlockFocusを確認。

# 共通背後斜め上三人称カメラ

- [x] PC・スマホ共通でカメラ位置をプレイヤー背後の斜め上へ固定し、敵ロックオンは注視点補助に限定する。
- [x] ロックオン中もプレイヤー背後基準の水平オフセットと上方視点を維持し、敵方向へカメラ位置が反転しないようにする。
- [x] タッチスワイプとPCマウス視点操作を背後斜め上の範囲内で動作させる。
- [x] PC横画面・スマホ縦画面・1対1連戦・lockAuditを検証し、型チェック、テスト、ビルドを完了する。

- [x] PC横画面でマウス視点操作を行い、背後斜め上カメラの水平オフセットと上下角制限が維持されることを監査ログで確認する。canvas pointer経路をVitestで確認し、PC画面を再検証。

- [x] PC横画面でマウスlook入力を実際に発生させ、lockAuditにorbitOffsetとbetaの変化を記録する。dx=180/dy=-160でorbitOffset=-0.720、beta=1.140を確認。
- [x] ロック中カメラのorbitOffset上下限とbeta上下限を専用回帰テストで検証する。CameraRigの上下限テストを追加。

# ブラウザゲーム敵対的検証

- [x] 入力競合、連打、同時アクション、ポインター解除を含む操作境界を検証する。InputManagerとCameraRigのVitestで確認。
- [x] 攻撃相殺、ガード、カウンター、怒破、撃破直前の状態遷移を検証する。同時監査で相殺・just guard・counterを確認。
- [x] 敵撃破、NEXT CHALLENGER、ロックオン解除・再取得の連戦遷移を検証する。lockAuditで確認。
- [x] PC・スマホのカメラ、HUD、タッチ領域、画面サイズ境界を検証する。1280x720と320x568で確認。
- [x] ブラウザERROR/WARN、型チェック、Vitest、本番ビルドを確認し、発見事項を影響度付きで報告する。レポートを追加。

- [x] 敵対的監査へ怒破発動と撃破直前ケースを追加し、他の戦闘分岐と干渉しないことを確認する。怒破開始、HP=1、通常被弾を確認。
- [x] NEXT CHALLENGER幕間のchallengeVisible遷移を監査ログで記録する。撃破時のtrueと次敵スポーン時のfalseを記録。
- [x] 敵対的検証レポートに各発見事項の影響度を明記する。High/Lowを記載。

- [x] 怒破・撃破直前を相殺・ジャストガード・カウンター監査と同時実行し、戦闘分岐が破綻しないことを確認する。同時ログで全分岐の開始・成立を確認。

- [x] High: 撃破直前に次敵がNEXT CHALLENGERの待機時間を待たずにスポーンしうる更新順の問題を修正し、幕間時間を回帰テストで固定する。RoundFlowで回帰確認。

# モバイル初回読み込み最適化

- [x] 本番バンドルを分析し、初期画面とBabylon.jsゲーム実行部のサイズを計測する。初期541,254 bytes、ゲーム2,259,321 bytes。
- [x] 初期ランディング画面からゲーム画面を遅延読み込みし、Babylon.jsをゲーム開始後に読み込む。初期JavaScriptは従来比90.0%削減。
- [x] 遅延読み込み中のモバイル・PC向け起動表示と失敗時の再試行導線を実装する。
- [x] ビルド出力、初期画面、ゲーム開始、PC横画面、スマホ縦画面、型チェック、Vitestを検証する。Vitest 15件、ERROR/WARN/HTTP 4xx/5xxなし。

- [x] 非demoで「ARENAへ入る」を押し、ロード表示を経てゲームが開始する通常経路をPC/スマホで確認する。launchAuditでPC/320x568を確認。
- [x] 遅延読み込み失敗時の再試行導線をテスト可能にし、再試行で通常起動へ復帰できることを確認する。forced failure→retry requested→game startedを確認。

# 次敵GLBバックグラウンドプリロード

- [x] 次ラウンドの敵GLBとアニメーションをNEXT CHALLENGER表示中にバックグラウンドで準備する。PreloadAuditでcrocodile readyを確認。
- [x] プリロード済みモデルを次敵スポーン時に再利用し、重複読込と未使用ノードの蓄積を防ぐ。consumed crocodile→requested gorillaを確認し、一体分のみ保持。
- [x] プリロード失敗時は既存の通常ロードへ安全にフォールバックする。failureAuditでfallback crocodileを確認。
- [x] 連戦、ロックオン、モバイル、PC、型チェック、Vitest、本番ビルドでプリロードの動作を検証する。PC/320x568、Vitest 17件、build成功。

# 起動遅延・スマホ移動・固定三人称カメラ修正

- [x] ゲーム開始直後は敵を出現させず、3秒後に最初の敵を出現させる。RoundFlow回帰テストとlaunchAuditで確認。
- [x] スマホのDパッド操作をプレイヤー移動入力へ確実に接続し、カメラスワイプ面と干渉しないようにする。Dパッド入力をカメラ面から分離し、回帰テストでlook=0を確認。
- [x] PC・スマホ共通でカメラをプレイヤー背後斜め上の三人称位置へ固定し、タッチ・マウス入力で位置が逸脱しないようにする。背後方位・半径・仰角を固定。
- [x] 起動遅延、スマホ移動、固定カメラをPC/スマホ実操作、Vitest、型チェック、本番ビルドで検証する。PC/320x568、Vitest 19件、build成功。
 

- [x] GitHubリポジトリ `songen_wo_kakeyouka3` へ現在の全実装・設定・テスト・ドキュメント・アセット参照を完全移行し、移行差分を検証する
